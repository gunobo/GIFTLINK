# Docker Compose용 MySQL 자격 증명 (docker-compose.yml이 이 값을 읽음)
# 이 파일을 .env로 복사한 뒤 실제 값으로 바꿔서 사용하세요. .env는 git에 커밋되지 않습니다.
MYSQL_DATABASE=giftlink
MYSQL_USER=giftlink
MYSQL_PASSWORD=change-me
MYSQL_ROOT_PASSWORD=change-me-root
